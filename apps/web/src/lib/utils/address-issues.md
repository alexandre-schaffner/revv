The reviewer has selected the following issues from the PR walkthrough.
Please address each one as a separate commit:

{{ISSUES_LIST}}
For each issue:
1. `Read` the affected file to confirm the code matches the description.
2. Apply the fix with `Edit` (or `Write` for new files).
3. Stage and commit with a conventional-commit message:
   `git add <paths> && git commit -m "fix(<scope>): <imperative summary>"`
4. Move to the next issue.

After all fixes, summarise what you committed and any tradeoffs you chose.
You do **not** need to push — the reviewer will inspect your commits locally.
